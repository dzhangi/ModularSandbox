<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/SplashFragment">

    <fragment
        android:id="@+id/SplashFragment"
        android:name="com.sandbox.modularsandbox.ui.screen.splash.SplashFragment"
        android:label="@string/first_fragment_label">
        <action
            android:id="@+id/action_SplashFragment_to_SecondFragment"
            app:destination="@id/SecondFragment" />
    </fragment>
    <fragment
        android:id="@+id/SecondFragment"
        android:name="com.sandbox.modularsandbox.ui.screen.login.LoginFragment"
        android:label="@string/second_fragment_label">

        <argument
            android:name="test_arg"
            android:defaultValue="999"
            app:argType="integer" />
    </fragment>
</navigation>